/* ch04-exam-20 */
#include <stdio.h>
#include <stdlib.h>
#include <conio.h>

int main()
{
    char ch;

    printf("請輸入一個字元:");
    ch = getche();
    printf("\n輸入的字元是:");
    putchar(ch);


    printf("\n請輸入一個字元:");
    ch = getche();
    printf("\n輸入的字元是:");
    putchar(ch);

    return 0;
}